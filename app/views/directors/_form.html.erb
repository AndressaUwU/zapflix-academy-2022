<%= form_with(model: director) do |form| %>
  <% if director.errors.any? %>
    <div style="color: red">
      <h2>Foi encontrado <%= director.errors.count%> erro: </h2>

      <ul>
        <% director.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='form-group'>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "dark-gray-5 form-control"%>
  </div>

  <div class="btn-right">
    <%= link_to "Cancelar", directors_path, class: "btn cancel btn-right" %>
    <%= form.submit "Salvar", class: "btn primary btn-right" do %>
    <%= link_to director_path %>
    <% end %>
  </div>
<% end %>
